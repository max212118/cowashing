<div class="container">
	<%= simple_form_for [ @machine, @appointement ] do |f| %>
		<%= f.input :begining_hour%>
		<%= f.input :done%>
		<%= f.button :submit, "envoyer", class: "hdhd" %>
	<% end %>
</div>
