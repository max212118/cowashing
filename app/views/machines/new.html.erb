<div class="container">
	<%= simple_form_for @machine do |f| %>
		<%= f.input :brand%>
		<%= f.input :capacity%>
		<%= f.input :location%>
		<%= f.input :price%>
		<%= f.button :submit%>
	<% end %>
</div>

